## Context
- Stage: tasks
- Feature: 010-frontend-chat-ui
- Prompt: Generate implementation tasks for the Docusaurus frontend chat UI feature based on the spec and plan.

## Response
- Wrote `specs/010-frontend-chat-ui/tasks.md` with:
  - Phase 1 tasks for non-stream MVP (`RagChat` component, state, POST `/chat` wiring).
  - Phase 2 tasks for docs integration (`RagChatPanel`, MDX embedding, build/tests).
  - Phase 3 tasks for streaming support, UX polish, and error handling.

## Tests
- Not applicable (tasks stage).


