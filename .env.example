# ===================================================================
# Environment Variables Template
# AI-Native Physical AI & Robotics Textbook Platform
# ===================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to `.env` in the repository root
# 2. Fill in your actual API keys and credentials
# 3. NEVER commit the `.env` file to version control
#
# All variables are OPTIONAL in the initial scaffold phase.
# The application will start with warnings for missing credentials.
# ===================================================================

# ===================================================================
# OpenAI Configuration
# ===================================================================
# Purpose: AI-powered features (chatbot, translations, embeddings)
# Where to get: https://platform.openai.com/api-keys
# Format: Starts with "sk-"
# Required: No (in initial phase)
OPENAI_API_KEY=sk-your-openai-api-key-here

# ===================================================================
# Qdrant Vector Database
# ===================================================================
# Purpose: Semantic search and RAG (Retrieval-Augmented Generation)
# Where to get: https://cloud.qdrant.io/ (create cluster)
# Format: Full URL with port (e.g., https://xyz.qdrant.io:6333)
# Required: No (in initial phase)
QDRANT_URL=https://your-instance.qdrant.io:6333
QDRANT_API_KEY=your-qdrant-api-key-here

# ===================================================================
# Neon Serverless Postgres Database
# ===================================================================
# Purpose: User data, authentication, content storage
# Where to get: https://neon.tech/ (create project)
# Format: PostgreSQL connection string
# Example: postgresql://user:password@ep-cool-darkness-123456.us-east-2.aws.neon.tech/dbname?sslmode=require
# Required: No (in initial phase)
DATABASE_URL=postgresql://user:password@host.neon.tech/database

# ===================================================================
# BetterAuth Configuration
# ===================================================================
# Purpose: User authentication and session management
# Format: Random 32+ character string
# Generate: `openssl rand -base64 32` or use password generator
# Required: No (in initial phase)
BETTERAUTH_SECRET=your-32-character-secret-key-for-auth

# ===================================================================
# SMTP Email Service
# ===================================================================
# Purpose: Email verification, password reset, notifications
# Where to get: Gmail, SendGrid, Mailgun, AWS SES
# Gmail setup: https://support.google.com/accounts/answer/185833
# Required: No (in initial phase)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-app-password-here

# ===================================================================
# Application Configuration
# ===================================================================
# Environment: development | production | test
# Ports: Frontend default 3000, Backend default 8000
NODE_ENV=development
PORT_FRONTEND=3000
PORT_BACKEND=8000

# ===================================================================
# Notes
# ===================================================================
# - In development: All API keys are optional, app will start with warnings
# - In production: Required keys must be set, app will fail to start without them
# - Security: Never commit actual credentials to version control
# - Updates: Restart backend after changing environment variables
# ===================================================================
