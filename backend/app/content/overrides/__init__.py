"""
Chapter Overrides

Optional chapter-specific overrides for tone, difficulty, formatting, and prompt modifications.
Overrides must not break the global AI block contract structure.
"""

# Override files are loaded dynamically by runtime engine
# Example override file structure:
# CHAPTER_OVERRIDES = {
#     "tone": "enthusiastic",
#     "difficulty": "beginner",
#     "formatting_style": {
#         "max_explanation_length": 500
#     },
#     "prompt_modifications": {
#         "ask-question": "Use simple language and provide examples."
#     }
# }

