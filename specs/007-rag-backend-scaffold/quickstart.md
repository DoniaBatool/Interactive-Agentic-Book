# Quickstart: RAG Backend Scaffold

1) Install deps: `pip install -r backend/requirements.txt`
2) Run locally: `uvicorn backend.app.main:app --reload`
3) Test health: curl `http://localhost:8000/health`
4) Verify frontend unaffected: `npm test`

