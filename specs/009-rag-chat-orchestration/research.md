# Research Notes: RAG Chat Orchestration

Choices:
- Retrieval top-k: default 5
- Chat model: default gpt-4o-mini (configurable)
- Filters: chapter/section metadata; pass as payload filters
- Streaming: chunked response via FastAPI

